import { LoginModel } from '../models/Login.model';
import { SHOW_LOGIN_MODAL, HIDE_LOGIN_MODAL } from './Actions';

/*export const showLoginModal = (item: LoginModel.LoginModalOpen) => ({
  type: SHOW_LOGIN_MODAL,
  payload: item,
});

export const hideLoginModal = (item: LoginModel.LoginModalOpen) => ({
    type: HIDE_LOGIN_MODAL,
    payload: item,
  });
*/